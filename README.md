# General info

Created based on minimal template with React Router v7.

For local testing create local .env file with:
```
APP_ENV=development
VITE_KITTENS_URL=https://placekittens.com/
```

then run:

```
pnpm i
pnpm dev
```